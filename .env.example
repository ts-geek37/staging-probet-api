# App
PORT=5000
ENV=dev
TOKEN=secret 

# Primary database connection (used by app + Drizzle)
DATABASE_URL=postgresql://postgres:root@localhost:5432/probet
CLERK_WEBHOOK_SECRET=
CORS_ORIGINS=http://localhost:3000